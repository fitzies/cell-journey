{
  "name": "cell-journey",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "pnpm --filter mobile start",
    "build": "echo \"No production build configured yet\"",
    "lint": "pnpm --filter mobile lint",
    "typecheck": "pnpm --filter mobile exec tsc --noEmit --pretty false && pnpm exec tsc -p convex/tsconfig.json --noEmit --pretty false"
  },
  "dependencies": {
    "@auth/core": "0.37.0",
    "@convex-dev/auth": "^0.0.92",
    "convex": "^1.39.1"
  }
}
